#include <stdio.h> 

int main()
{ 
    printf("the size of int %d \n",sizeof(int));
    printf("the size of int %d \n",sizeof(float));
    printf("the size of int %d \n",sizeof(double));
    printf("the size of int %d \n",sizeof(char));
    
    
 
return 0;
}